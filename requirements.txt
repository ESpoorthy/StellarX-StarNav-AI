# ─────────────────────────────────────────────────────────────────────────────
# StellarX-StarNav-AI — Core Dependencies
# Python 3.11+  (torch requires Python <=3.12 — not yet available on 3.14)
#
# Install with:  pip install -r requirements.txt
# ─────────────────────────────────────────────────────────────────────────────

# Numerical computing
numpy>=1.26

# Data handling
pandas>=2.0

# Computer vision / image processing
opencv-python>=4.9,<5.0

# Plotting and visualization
matplotlib>=3.8,<4.0

# Scientific computing (Gaussian PSF, image convolution for star-field generation)
scipy>=1.11

# Machine-learning (Phase 3 active classifier: RandomForest/KNN/MLP + metrics)
scikit-learn>=1.4,<2.0

# Deep learning framework — PyTorch
# (CPU-only build; swap for torch with CUDA extras for GPU training)
# torch>=2.2,<3.0        # install separately: pip install torch
# torchvision>=0.17,<1.0 # install separately: pip install torchvision

# Configuration management
pyyaml>=6.0,<7.0

# Interactive web application
streamlit>=1.35,<2.0

# Image I/O (PNG/TIFF read-write without OpenCV overhead)
Pillow>=10.0

# Testing
pytest>=8.0,<9.0

# Notebook support (optional, for running .ipynb files)
jupyter>=1.0,<2.0
ipykernel>=6.29,<7.0

# ── Phase 7 — Demonstration UI additions ─────────────────────────────────────

# Watchdog backend for Streamlit auto-reload on Windows (optional but recommended)
watchdog>=4.0,<5.0
